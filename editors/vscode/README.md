# FemLang VS Code extension starter

This directory contains the first editor-support layer for FemLang. It provides syntax highlighting and basic editor configuration without requiring a language server.

The extension can later grow to include:

- diagnostics from the FemLang parser
- autocomplete
- go-to-definition
- formatting
- a debugger adapter
- language-server support
