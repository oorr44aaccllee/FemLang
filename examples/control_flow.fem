let score = 10

if score == 10:
    "slay"
else:
    "try again"

fn classify(n):
    if n < 0:
        return -1
    elif n == 0:
        return 0
    else:
        return 1

print(classify(-5), classify(0), classify(42))